<h1>The Gatopan Positions (after Nvidia adquisition)</h1>
<div>
    <table>
      <thead>
        <% Position.column_names.each do |header| %>
          <th><%= header %></th>
        <% end %>
      </thead>
      <tbody>
        <% @positions.each do |position| %>
          <tr>
            <% position.attributes.each do |k,v| %>
            <td><%= v %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>
